#!/bin/bash
# Bismark Devices Management Daemon wrapper
#
# author: walter.dedonato@unina.it

# Load configuration file
. ~/etc/bdm.conf

# Aliases
query(){ sqlite3 -list $BDM_DB "$1"; }

# Help screen
# $1 = command
function help() 
{
	cat <<-end
	Syntax:

	    $(basename $0) [options] <start|stop|restart|info>
	
	Options:
	
	    -l <prb_port>    Port number to listen on for probe packets (default: $PROBE_PORT)
	    -h               Print help screen
	end
	exit
}

## Main ##

# Check directories
mkdir -p ~/var/log/devices/

# Check DB
if [ ! -e $BDM_DB ]; then
	query "CREATE TABLE devices(
		id      TEXT PRIMARY KEY,
		version INTEGER, 
		user    TEXT, 
		ip      TEXT, 
		ts      INTEGER
	       );"
	query "CREATE TABLE tunnels(
		id      TEXT,
		port    INTEGER, 
		ts      INTEGER
	       );"
	query "CREATE TABLE messages(
		'from'  TEXT,
		'to'    TEXT, 
		msg     TEXT
	       );"
	query "CREATE TABLE targets(
		ip       TEXT PRIMARY KEY,
		cat      TEXT, 
		zone     TEXT,
		free_ts  INTEGER,
		curr_cli INTEGER,
		max_cli  INTEGER
	       );"
	query "CREATE TABLE capabilities(
		ip       TEXT,
		service  TEXT,
		info     TEXT
	       );"
	query "CREATE TABLE mtypes(
		type       TEXT PRIMARY KEY,
		exclusive  INTEGER 
	       );"
fi

# Parse command-line
while getopts 'l:h' flag; do
	case $flag in
	l)
		PROBE_PORT=$OPTARG
	;;
	h)
		help
	;;
	*)
		echo "Unknown option: $flag $OPTARG"
		help
	;;
	esac
done
shift $(( OPTIND - 1 ))

pid=$(pgrep bdmd.real)
case $1 in
start)
	if [ $pid ]; then
		echo "bdmd already running"
	else
		echo -n "Starting bdmd..."
		bdmd.real $PROBE_PORT >> $BDMD_LOG_FILE &
		sleep 1
		[ $(pgrep bdmd.real) ] && echo "done" || echo "error"
	fi
;;
stop)
	if [ $pid ]; then 
		echo -n "Stopping bdmd..."
		kill $(pgrep bdmd.real)
		sleep 1
		[ $(pgrep bdmd.real) ] && echo "error" || echo "done" 	
	else
		echo "bdmd not running"
	fi
;;
restart)
	$0 stop
	$0 -l $PROBE_PORT start 
;;
info)
	if [ $pid ]; then
		port=$(sudo netstat -lanup | awk -F':| *' '/'$pid'\//{ print $5 }')
		echo "bdmd listening on $port (pid $pid)"
	else
		echo "bdmd not running"
	fi
;;
*)
	help
;;
esac

